Create a new Database:
======================


    Default DB as same as Role created for that user.
    Role will attempt to connect to a database which is also called narayanan by default.

    * createdb narayanan
    * sudo -u postgres createdb narayanan